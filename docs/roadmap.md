# Roadmap

- **v0.3 (shipped):** PII-redact guard, richer `validateArgs`, `McpWorksError`, quality gates.
- **v0.4:** `npm create` scaffolder, rate-limit guard option, more examples.
- **v1.0:** stable API freeze after 1 month of no breaking feedback, OTel tracing, registry audit metadata.
